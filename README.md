```
$ whoami
```
Abdo. Building things until I run out of excuses not to.

```
$ cat philosophy.txt
```
Sometimes building isn't about creating a project. It's about having the guts to finish one.

My repo history is proof of the alternative. I've started more things than I'll ever admit to. What separates the ones below from the graveyard isn't a better idea, it's that I shipped them anyway, half convinced they were dumb, and finished regardless.

```
$ cat currently.txt
```
Studying Computer Science (UoPeople) and Computer Software Engineering (1337/42 Network).

An English Literature degree happened somewhere in there too. No, I don't have a tidy explanation for why. It's turned out useful in ways a CS degree alone wouldn't have.

```
$ ls shipped/
```

**RoundTable** — Claude, Gemini, and ChatGPT in one shared terminal conversation, actually arguing with each other.
`built to understand multi model orchestration. found out Gemini 3 silently burns your token budget on "thinking." the hard way.`
[→ github.com/MABFLOW/RoundTable](https://github.com/MABFLOW/RoundTable)

**LibSpring** — modular backend library across Node.js, Python, and Spring Boot, full JWT/Spring Security auth.
`got tired of resolving the same backend problem in three different stacks`

**RAG Against the Machine** — RAG pipeline, BM25S + Qwen3-4B-Instruct, CLI driven.
`wanted to understand retrieval instead of trusting a framework to do it for me`

**TM** — CLI task manager, Python + SQLite.
`Notion was more overhead than the tasks it was tracking`

**MindScript (backend)** — personal finance tracker, Spring Boot.
`wanted to stop guessing where my money went`

```
$ cat stack.txt
```
Python · JavaScript/TypeScript · Java · C
React · React Native · Node.js · FastAPI · Spring Boot
Claude API · OpenAI API · Gemini API
Linux

```
$ cat contact.txt
```
[LinkedIn](#) · [Email](#)

```
$ history | tail -1
```
`# some of those commits were 2am decisions I still stand by`
